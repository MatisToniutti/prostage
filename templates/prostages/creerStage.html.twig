{% extends 'base.html.twig' %}
{% block body %}
    <h2>Création d'un stage</h2>
    {{form_start(vueFormulaire)}}
        {{ form_row(vueFormulaire.titre)}} <br>
        {{ form_row(vueFormulaire.mission)}} <br>
        {{ form_row(vueFormulaire.email)}} <br>
        {{ form_row(vueFormulaire.entreprise)}} <br>
    <button type="submit" class="btn btn-dark">Enregistrer</button>
    {{form_end(vueFormulaire)}}

{% endblock %}