<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <title>{% block title %}Prostage{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        <div class="container">
            <div class="row">
                <div class="col offset-5">
                    <br>
                    {% block prostage %} <a href="{{ path('accueil') }}" class="btn btn-dark btn-lg">Prostages</a> {% endblock %}
                </div>
            </div>
                {% if is_granted('ROLE_USER') %}
                    <a class="btn btn-primary" href="{{path('app_logout')}}">{{ app.user.email }} Déconnexion</a>
                {% else %}
                    <a class="btn btn-primary" href="{{path('app_login')}}">Connexion</a> 
                {% endif %}
        </div>
        
    </head>
    <body>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
        {% block body %}{% endblock %}
    </body>
</html>

 